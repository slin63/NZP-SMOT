using System;
using System.Collections.Generic;

// More or less C# translations of the VBA code in the spreadsheet.
// The original functions are pretty large. Trying to modulate it.
// Will be directly reusing some of the original function names.
//      Some don't make much sense so they'll be subject to change.
namespace SMOTCalc
{
    public class AMain
    {
        public static void Main(string[] args)
        {
            Test.TestTest.writeTest();
        }

        public void AMainMaster(bool simMethod)
        {
            // This is where it gets weird . . .

            // Count number of watersheds.
        }
    }
}

